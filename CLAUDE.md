@AGENTS.md

# ENGLISH700 — プロジェクトルール

TOEIC700点突破を目指す社会人向け英単語アプリ。DMM生成AI CAMP 卒業課題。

**設計の正典は `docs/spec.md`。** 実装前に必ず読むこと。
配布データの実測値は `docs/data-findings.md`、判断の記録は `docs/decisions.md`。

---

## テストのルール

**進め方**
- テストが失敗したまま、次の作業（コミット・デプロイ・別機能の実装）に進まない。
  まず失敗を修正し、全テストが green になってから次へ進む。
- src/lib/ 配下を変更したら、必ず `npm test` を実行して green を確認してから完了とする。
- 完了報告には必ず含める：テスト件数／green・red／`tsc --noEmit`／`lint`／`build`

**失敗したときの手順（★ここが最重要）**
- **勝手に実装を書き換えない。** 原因と修正方針を先に提示し、承認を得てから修正する。
- **原因は推測で断定しない。実測で特定する。**
  ログ・診断コード・最小再現のいずれかで裏を取ってから原因だと言う。
- **仮説が複数あるなら、それを切り分ける確認方法を先に示す。**
  「AだからBを直す」ではなく「AかCか。切り分けるにはDを見ればよい」の形で出す。
- **修正が複数箇所に及ぶ場合は、影響範囲を先に列挙する。**
- **緑になった理由を説明できない緑は信用しない。**
  直ったのではなく別の要因で通っただけ、の可能性を潰す。

**細工の禁止**
- テストを通すために、テストの期待値・アサーションを書き換えない。
  テストが失敗したら原則としてプロダクトコード側を直す。
  テスト仕様そのものが誤っている／要件が変わった場合のみ、先に理由を説明し承認を得る。
- 失敗を回避する目的で、テストケースを削除・コメントアウト・スキップしない。
  （`it.skip` / `describe.skip` / `test.skip` / `it.only` での絞り込みを含む）
  スキップが必要な場合のみ、理由を明記して承認を得てから行う。

**新規テストを書くとき**
- **落ちることを確認してから実装に入る。** 最初から緑のテストは何も守っていない。

**テストできる境界**
- テストする：プロンプト組み立て関数（＝何を渡しているか）／誤答生成・原因分類・
  セッション組み立て／JSONパース失敗時のフォールバック
- テストしない：LLMが返した文章の中身（点滅するテストになるため）
- 境界値と対象は `docs/spec.md` と `docs/data-findings.md` を参照する。

## 進捗管理のルール

- 実装が完了したら、必ず `docs/進捗管理.md` の該当項目を更新する。
- 該当項目がない場合は、新しい項目を追記した上で完了状況を記録する。
- 更新時は、ファイル冒頭の「最終更新」の日付も併せて更新する。
- docs に要件がない機能を実装した場合、コード修正後に `docs/spec.md` も更新する。

## セキュリティ

- APIキーはコードに書かない。必ず環境変数
- 環境変数名に `NEXT_PUBLIC_` を付けない
- クライアントから `api.anthropic.com` を直接叩かない。必ず API Route 経由
- `.env*` は削除・変更しない

### シークレットの扱い（2026-08-16 追加）

**NGなのは秘密の値そのものだけ。それ以外は全部AIが書く。**

| Claude Code が書く | 手作業に残す |
|---|---|
| 空ファイル・ディレクトリの作成 | 秘密の値を貼る、この一点だけ |
| `git check-ignore -v` での確認 | |
| 存在確認・`grep -c` での件数カウント | |
| 秘密でない行（`AI_MODEL=claude-sonnet-5` 等） | |
| 値が空の行（`ANTHROPIC_API_KEY=` まで） | |

つまり `.env.local` は、キー名と `=` まで揃った状態で Claude Code が用意し、
そうちゃんは値を貼るだけにする。

**過剰防衛はそれ自体がコスト。** 安全側に倒しすぎて手作業を増やすと、
本当に守るべき一点（値が承認画面・ログ・スクショを通ること）への注意が薄まる。

## 配布データ

- **`src/data/wordlist.json` は配布データ。絶対に書き換えない**
- `similar` は「カンマ区切りの**文字列**」。配列ではない
  （配列として `.forEach` すると即 TypeError）
- `pos` の区切りは `/`、`meaning` の区切りは `・`。**取り違えない**
  （`pos` に `・` は0件、`meaning` に `/` は0件）
- 複合品詞は**先頭トークンを主要品詞**とする（動詞/名詞→動詞、名詞/動詞→名詞、名詞句→名詞）
- `similar` は類義語であり混同語ではない。誤答選択肢に使わない（ブロックリストには使う）
- 誤答候補のブロックリストは**5条件**
  1. 同じ id
  2. 同じ `word`（word重複が26語あるため。id ではなく word で判定）
  3. 出題語の `similar` に含まれる
  4. 候補の `similar` に出題語が含まれる／`similar` に共通語がある
  5. `meaning` に共通要素がある（`・` 分割の集合交差）
- 1セッションの中で `word` が重複しないようにする
- **誤答フォールバックは必須。** 例外処理ではなく通常フロー
  - `id=4 available`（形容詞・L1）… 誤答B が構成不能
  - `id=48 approximately`（副詞・L2）… 誤答B も 誤答C も構成不能

## 環境の罠（Obsidian 環境の罠/ からの転記。リポジトリ単体で完結させる）

- **Vercel/Next.js**：Server Component で `new Date()` による「今日」の計算をしない（VercelはUTC）
- **Vercel/Next.js**：Server Component でシャッフル・乱数を実行しない（ハイドレーション不一致）
- **Vercel/Next.js**：環境変数はVercelダッシュボードにも設定する。変更後は再デプロイが要る
- **iCloud-Drive**：リポジトリは `~/Projects/` 配下に置く
- **Vitest**：「Failed to start forks worker」は node_modules 破損。`npm ci` で直る。設定をいじり始めない

## 進め方

- 実装前に「変更するファイルと影響範囲」を提示する
- **推測で書かない。読めなかったら「読めなかった」と明言する**
- 違っていたら違っていたと明言する
- **「この方法では見えないもの（限界）」を必ず添える**
