# 設計判断の記録（decisions）

なぜそう決めたか。**採用しなかった案とその理由も残す。**
確定値そのものは `docs/spec.md`、実測値は `docs/data-findings.md`。

---

## 1. 課題②に一点集中した（①③は既存要素で成立させる）

業界3課題のうち **②間違えた原因がわからない** を正面から解く。

**理由**：作業時間5〜10時間で3つ全部に手を出すと全部が薄くなる。評価項目3は「少なくとも1つ」を求めている。加えて `pos` が全語にある＝運営が判定材料を先に揃えている＝②をやれというシグナルと読んだ。

**①③を捨てたわけではない。** ②を解く過程で自然に成立する範囲で応える。**専用画面も新機能も作らない。**

| 課題 | 対応 | 使う要素 |
|---|---|---|
| ① 覚えた≠使える | 副次的 | review_cards の usage_note（similar）と example（example_scene） |
| ② 原因がわからない | **正面** | 誤答の構成設計＋cause 確定＋AIの説明 |
| ③ 実感がない | 数字で | SCORE・即答率・テンポ・復習カードが減る |

---

## 2. 出題形式を「英単語 → 日本語4択」にした

**採用しなかった案**：「日本語 → 英単語」

**理由**：`similar` が類義語なので、日本語→英単語だと選択肢に similar 語を置いた瞬間に「両方正解」になる。

```
「提供する・与える」→ provide / supply / offer / give
```

4つとも正解になってしまう。英単語→日本語なら、日本語 `meaning` の重複だけ管理すればよい（→ 判断4）。

---

## 3. `similar` を誤答選択肢に使わない

**理由（実測）**：

| 項目 | 数値 |
|---|---|
| `similar` の総出現数 | 860 |
| **うち300語リスト内に存在する出現** | **101（11.7%）** |
| `similar` が1つもリスト内にない単語 | 219 / 300 |

※「101」は**出現ベース**（860出現のうち101回がリスト内の語）。ユニーク語で数えると692語中70語（10.1%）。どちらも同じ事実を別の粒度で見たもの。

つまり `similar` の語の大半には**日本語訳が存在しない**。選択肢として並べることすらできない。

**→ 誤答生成には使わない。フィードバックの「使い分け説明」の材料として使う。**
**→ ただしブロックリストとしては使う**（両方正解を防ぐため）。

補足PDFは `similar` を「混同しやすい類似語」と説明しているが、実物は**同義語**だった。ここを読み違えると設計全体が崩れる。

---

## 4. ブロック条件5（`meaning` の共通要素）を追加した

**当初の4条件では防げない「両方正解」が6組残っていた**（実測）。

| 出題語 | 誤答候補になりうる語 | 共通する意味 |
|---|---|---|
| 26 announce「発表する・告知する」 | 28 present「発表する・出席している」 | 発表する |
| 63 coordinate「調整する・まとめる」 | 158 calibrate「較正する・調整する」 | 調整する |
| 73 facilitate「促進する・容易にする」 | 205 stimulate「刺激する・促進する」 | 促進する |
| 73 facilitate | 223 expedite「促進する・迅速に処理する」 | 促進する |
| 205 stimulate | 223 expedite | 促進する |
| 260 upfront「前払いの・率直な」 | 267 forthcoming「今後の・率直な」 | 率直な |

**6組すべてが同じ主要品詞**なので、誤答B・誤答Cの候補として実際に選ばれる。

**これは単なる不良問題ではない。** 出題語が `facilitate` のとき誤答に `expedite` が出ると、鈴木さんが「促進する」を見て迷うのは**正しい判断**であり、それを `weak_memory`（記憶が薄い）と診断するのは**誤診断**になる。本アプリの唯一の売りが「原因が確定すること」なので看過できない。

**実測した追加コスト：**

| | 平均ブロック数 | 誤答Aが空 | 誤答Bが空 | 誤答Cが空 |
|---|---|---|---|---|
| 4条件 | 1.73語/出題語 | 0語 | 2語 | 1語 |
| **5条件（採用）** | 1.77語/出題語 | 0語 | 2語 | 1語 |

バケットが空になる語は**1語も増えない**。実質タダで6組の誤診断を防げる。

---

## 5. 複合品詞は「先頭トークン」を主要品詞とする

`pos` には「動詞/名詞」のような複合表記が35語ある。誤答A「異なる pos」の判定方法が仕様書に未定義だった。

**採用**：正規化後の**先頭トークンのみ**で判定する。

**理由1**：「動詞/名詞」15語と「名詞/動詞」11語が**別々に存在する**＝作成者が主要用法を先に書いていると解釈できる。`meaning` の語順とも一致する。

**理由2**：全トークン交差方式（複合語を広く同一視する）は、**名詞と動詞の区別＝まさに `pos_mismatch` で測りたいものを潰す**。

**理由3（実測）**：誤答が組めない語の数

| 判定方法 | 誤答が組めない語 |
|---|---|
| **先頭トークン（採用）** | **2語** |
| 全トークン交差 | 7語（名詞句6語の誤答Cが全滅） |

先頭トークン方式のほうが精度も供給量も優れている。

---

## 6. 復習カードのキーを `id` にした（`word` ではない）

**採用しなかった案**：`word` をキーにして同じ英単語を1枚に統合

**理由**：配布データに `word` 重複が26語ある。同じ綴りでも意味が全く違う組がある。

```
overhead (121) 経費・間接費
overhead (287) 頭上の・上空の
```

`word` をキーにすると、「経費」の意味を覚えただけで「頭上の」も卒業扱いになる。**覚えていない語が復習対象から消える**のは、このアプリで最も避けたい事故。

`mutual`(192「相互の・共通の") と `mutual`(279「お互いの・双方の」) のように意味が近い組もあるが、**判定を単純に保つため一律で id 別カード**とする。

---

## 7. AI呼び出しを「10問終了後に1回だけ」にした

**採用しなかった案**：1問ずつ呼ぶ

| | 1問ずつ | 10問まとめて（採用） |
|---|---|---|
| 待ち時間 | 毎問発生 | ゼロ |
| APIコスト | 10回 | 1回 |
| **AIが見える範囲** | その1問だけ | **10問全体のパターン** |

**3つ目が本命。** まとめて渡すと「今日は品詞の取り違えが3回。動詞の名詞形で崩れる傾向がある」という**パターンレベルの指摘**ができる。1問ずつだと点の指摘しかできず、評価項目2が警戒する「汎用的な一般論」に落ちる。

鈴木さんは通勤電車で使う。毎問2〜3秒待たされたら、また3日で終わる。

---

## 8. AIに原因を推測させない

`cause` は**アプリ側で確定させてから渡す**。AIの役割は「確定した原因を鈴木さんの言葉で説明する」ことに限定する。

**理由**：普通の実装は原因推定までAIに丸投げするので一般論が返る。原因を先に確定させれば**必然的に個別化される**。評価項目1「必要な情報をセットで渡せているか」への直接の答えでもある。

同じ理由で `causeCounts`（原因ごとの件数）も**アプリが数えて渡す**。AIに数えさせると数え間違いが `pattern_summary` の説得力を直撃する。

`suggested_tempo` も同様に、AIの提案をアプリ側が**数値条件で再計算して検証**し、満たさなければ黙って捨てる。

---

## 9. モデルを `claude-sonnet-5` にした

**当初の指定**：`claude-sonnet-4-6`

**変更理由**：`claude-sonnet-4-6` は**前世代のSonnet**であり、**structured outputs の対応モデル一覧に含まれていない**（対応：Fable 5 / Opus 5 / Opus 4.8 / Sonnet 5 / Haiku 4.5）。JSONスキーマ強制は本設計の根幹（AI経路がパース失敗で死ぬのを防ぐ土台）なので、非対応モデルでは成立しない。

**Sonnet を選ぶという判断自体は変えていない。世代だけ現行に上げた。**

**API上の制約（実装時に効く）：**

- スキーマ強制は `output_config: { format: { type: "json_schema", schema } }`。旧 `output_format` は非推奨
- 全オブジェクトに `additionalProperties: false` が必要
- `maxLength` / `maxItems` / `minimum` などは**非対応**。「1〜2文で」「最大5枚」はプロンプト本文で指示する
- `temperature` / `top_p` / `top_k` は非デフォルト値を渡すと**400エラー**
- adaptive thinking がデフォルトON。`max_tokens` は thinking と本文の合算上限

環境変数 `AI_MODEL` で切替可能にしてあるので、遅ければ Haiku 4.5 と実測比較して切り替える。

---

## 10. 例文の事前一括生成を破棄した

**検討した案**：300語ぶんの例文を事前生成してJSONに焼く

**破棄した理由**：本設計では例文が `review_cards[].example` として**同じ1回の呼び出しで返る**ため不要。事前生成スクリプトを書く時間（約1時間）がまるごと浮く。

`example_scene` は300語に対して276種類あり、グループ化には使えない（実測）。例文生成・文脈提示の材料としてのみ使う。

---

## 11. 画面を「単一ページ遷移」にした

ホーム・出題・結果を `/` 内の React state 遷移にし、`/review` だけ別ルートにする。

**採用しなかった案**：`/`・`/quiz`・`/result`・`/review` をすべて別ルート

**理由**：セッション状態（出題語・回答・経過時間）を localStorage に中間保存する必要がなく、ハイドレーション不一致の考慮も減る。別ルートにすると Context か localStorage で状態を持ち回す必要があり、リロード時の復元も考えることになる。

**代償として、セッション途中で離脱すると結果が消える。** 1セット3〜5分なので実害は小さいと判断した（→ §15 限界）。

---

## 12. 問題数を定数化した（UIに選択肢は出さない）

```ts
export const QUESTIONS_PER_SESSION = 10;
export const LEVEL_RATIO = { 1: 1, 2: 4, 3: 5 };
```

**コードのどこにも数値 `10` を直書きしない。** level配分も比率として持ち、問題数から枠数を算出する。

**理由**：提出版は必須要件どおり10問固定。ただし提出後の自分用カスタム版では20問にしたい。**定数1行の変更で済む構造**にしておく。

実測で 20問→2:8:10 に自動追従することを確認済み。

**UIに問題数の選択肢は出さない。** 選ばせると「何問がいいか」を考える手間が増え、スキマ時間で迷わず使うという要件に反する。

---

## 13. 「もう1セット」を入れた（問題数は可変にしない）

結果画面に「もう1セット」ボタンを置き、押すとセッション状態をリセットして新しい10問が始まる。

**理由**：

- 必須要件「1回のプレイが10問前後」は守る。**問題数を可変にはしない**
- 「もっとやりたい」は**問題数ではなく回数**で満たす
- 1セットごとに SCORE が出るので、**記録の比較可能性が保たれる**（2セット合算だと200点満点になり過去の100点満点と比較不能）
- 単一ページ遷移なので実装は状態リセットのみ

**セッション履歴は1セット＝1レコード。** AI呼び出しも1セット＝1回（もう1セットすれば2回目が発生する）。

前セットで出た語は除外しない。むしろ前セットで落とした語はカードができて優先度が上がっているため、2セット目に再登場して**そのセットで卒業できる**。これは「今日これをやれば前進している」実感に直結する意図した挙動。

---

## 14. アプリ名を ENGLISH700 にした

| 観点 | 判断 |
|---|---|
| 何のアプリか | 一目でわかる。審査員が数分で見る前提では明快さが最優先 |
| 課題文との一致 | 「TOEIC700点突破を目指す社会人向け」そのもの |
| 弱み | アプリの核（正誤の外側を見る仕組み）が名前に入っていない |

**弱みは `<title>` とトップページのタグラインで補う。** 名前で説明しきろうとせず、名前は指し示すだけにする。

---

## 15. 端数配分に最大剰余法（ハミルトン方式）を採用した

`LEVEL_RATIO = 1:4:5` は10問なら割り切れるが、問題数を変えると端数が出る（15問なら 1.5 / 6 / 7.5）。

**採用**：最大剰余法。小数部の降順に1枠ずつ配り、**同値のタイブレークは level の降順（L3 → L2 → L1）**。

**タイブレークをL3優先にした理由**：目的がTOEIC700突破であり、L3は150語＝プール最大かつ伸びしろが大きい。端数を易しい側に寄せると「今日やった手応え」が薄れる。L1は40語しかなく、枠を増やすと枯渇が早い。

実測結果：

| 問題数 | L1 | L2 | L3 |
|---|---|---|---|
| **10（提出版）** | 1 | 4 | 5 |
| 12 | 1 | 5 | 6 |
| 13 | 1 | 5 | 7 |
| 15 | 1 | 6 | 8 |
| **20（自分用）** | 2 | 8 | 10 |

---

## 16. 自作語の id は 1001 番台から

```ts
export const DISTRIBUTED_ID_MAX = 300;
export const CUSTOM_ID_BASE = 1001;
```

**採用しなかった案**：文字列キー（`"dist:12"` / `"custom:1"` など）

**理由**：

1. `WordId = number` のまま維持できる。文字列キーにすると配布JSONの `id: number` を毎回変換する層が要り、localStorage に既に書いた値との互換も壊れる
2. 1000 という境界に意味を持たせられる（`id < 1000` = 配布データ＝書き換え禁止、`id >= 1001` = 自作）。ログや復習カードを見た瞬間に出所が分かる
3. 1001 開始（1000 ではなく）で境界値のオフバイワン議論を避ける
4. 自作語は `src/data/custom-words.json` を新規作成して concat する。**配布ファイルは一切触らない**という制約を構造で守れる

**提出版では自作語を追加しない**（時間予算）。方針だけ確定させておく。

---

## ★未確定（実装後にここへ追記する）

### テンポ閾値 8 / 5 / 3 秒

| テンポ | 即答の閾値 |
|---|---|
| ゆっくり | 8秒（暫定） |
| ふつう | 5秒（暫定・既定） |
| はやい | 3秒（暫定） |

**この3つの数字には現時点で根拠がない。**

「TOEIC Part 5 は1問あたり約20秒、文法と文脈読解を含むので単語の意味想起に使える時間は3秒程度」という理屈は立てたが、**実測はしていない**。

閾値は SCORE・復習カードの卒業判定・`suggested_tempo` の発火条件すべてに効く中心的なパラメータなので、**実装後に自分で触って調整し、確定値と根拠をここに追記する**。

調整時に見るべきもの：

- 「ふつう」で自分の即答率が何%になるか（低すぎると常に迷い扱いになり卒業できない）
- 「はやい」が実際に達成可能か（不可能なら AI が提案しても意味がない）
- 通勤電車の揺れる環境でタップが遅れる分をどう見るか

---

## 2026-08-16

決めたことの記録。**内容は複製しない。正典は `docs/spec.md`。**

- tier内シャッフルの適用範囲を「同値の中だけ」に確定 → spec.md §5-3
- AIの役割・口調・禁止事項を確定（手順書 STEP 4-1 から転記）→ spec.md §10-8
- CardContent 各フィールドの内容を確定 → spec.md §10-9
- cause_label はアプリ側で決定し、レスポンススキーマから外す → spec.md §10-9
- example_en / example_ja の分割理由を明記 → spec.md §10-9
- review_cards の出力上限を5枚、優先順位を確定 → spec.md §10-10
- 検証層に V6 を追加。V2（検証）の後に slice(0,5) する順序を確定 → spec.md §10-5
- AI が cards に存在しない id を返した場合は無視する（卒業を復活させない）→ spec.md §10-5
- V5 の文字列長上限を6項目で確定（末尾クリップのみ）→ spec.md §10-5
- suggested_tempo の "normal" は数値条件が無いため条件2のみで通す → spec.md §10-4

### 実API疎通・本番デプロイを受けた確定（同日・後半）

- structured outputs はフィールドのみ強制する（枚数は弾けない）ことを敵対的テストで確認 → spec.md §10-11 b
- `max_tokens: 8000` は据え置き（5枚生成で output_tokens 1674・上限の20.9%）→ spec.md §10-11 c
- `maxDuration` を 30 → 60 に引き上げ。**504 を防ぐためであり待機時間の解決策ではない** → spec.md §10-12 判断1
- `AI_MODEL` の Haiku 4.5 切替は保留（usage_note の書き分けが失われうる）→ spec.md §10-12 判断3

### STEP 5（画面設計）で確定

- `analyzing` と `result` は**同一レイアウトの状態差**にする（画面を切り替えない）→ spec.md §12-6 a
- 待機中はアプリ側計算（SCORE・正解率・即答率・テンポ・cause内訳）を**すべて**出す → spec.md §12-6 b
- AI 到着前でも「もう1セット」を押せる。生成中の応答は破棄せずカードだけ保存する方針 → spec.md §12-6 c
- **ストリーミングしない。** カードは5枚同時到着（V2 の幻覚チェックが全件前提のため）→ spec.md §12-6 d
- 演出は0.1秒間隔で1枚ずつフェードイン。**待機中にスピナーを置かない** → spec.md §12-6 e
- `FEEDBACK_TIMEOUT_MS` を 25000 → **45000** に確定（判断2 の保留を解除）→ spec.md §12-6 f
- オフライン・圏外の fetch 失敗も pending 方式。**文言を区別しない** → spec.md §12-7
- 主要ボタンと4択は**画面下部に寄せる**（片手・立ち姿勢）→ spec.md §12-7
- 世界観を「**静かな道具**」と言語化 → spec.md §13-1
- タグラインを「**なぜ間違えたかが、わかる。**」に確定 → spec.md §13-2
- 差し色2色を緑系 `#4ade80` / 橙系 `#fb923c` に確定。**赤は使わない** → spec.md §13-3
- フォントはシステムフォント（Web フォントを読み込まない）→ spec.md §13-4

### STEP 5（`/frontend-design`）で確定

- デザイントークン（色9・余白8段・角丸2種）を確定。**影を使わず 1px ヘアラインで分離する** → spec.md §13-5
- 全トークンのコントラスト比を実測し、下限を `--text-mute` 5.0:1 に置いた → spec.md §13-5 a
- 日英混在の規則：**英単語・数値は等幅、日本語はサンセリフ** → spec.md §13-6 a
- **16px 未満のテキストを作らない。** 階層はサイズではなく余白と色で作る → spec.md §13-6 b
- `layout.tsx` の `Geist` / `Geist_Mono` は**外す**（§13-4 の保留を解除）→ spec.md §13-6 d
- レイアウト規則「**AI 由来のものは必ずアプリ側計算より下に置く**」（到着でレイアウトを動かさないため）→ spec.md §13-7 b
- 署名要素を **`ScoreStrip`（10問の目盛り。高さ＝得点10/5/0）** に確定。進捗バーを別に作らない → spec.md §13-8
- 「色だけに頼らない」を状態×文字の対応表に落とした。**文字列は必須表示物** → spec.md §13-9
- 動かすものはカード到着と選択肢押下の2つだけ。カウントアップ・シマーを置かない → spec.md §13-10 a
- **CSS 変数の定義ファイルは STEP 5 では作らない**（転記は STEP 6）→ spec.md §13-10 c

### STEP 6（UI実装）で確定

- `ScoreStrip` の凡例は **result / home で出し、quiz では出さない**（2026-08-17）→ spec.md §13-8
  quiz の目盛りは「進捗」として働き、回答後は選択肢の左に `正解` / `選択` の文字が出るので色だけに頼っていない。
  加えて 375×667 に凡例2〜3行を足すとドックが押し出され、§12-7「主要な操作と4択は下寄せ」が壊れる。
- `ScoreStrip` の**色の意味づけを「復習対象かどうか」から「正誤」に変更**（2026-08-17）→ spec.md §13-3・§13-8・§13-9
  正解(迷い)を `--attn` → `--ok` に変更。高さ 20 / 10 / 3px は据え置き。色で正誤・高さで精度の二層にする。
  緑=正解 / 橙=誤答 は凡例を読まなくても届くため。差し色は2つのまま、トークンの値も変えていない。
  - **採用しなかった案**：橙の用途そのものを「誤答・注意」に狭める。`--attn-fill`（復習対象の面）や
    `ReviewCard` の色設計まで波及し、未着手の段階4・5 を巻き込むため見送った。
    代わりに §13-3 に「`ScoreStrip` だけは色を正誤の二値に使う」という例外を1行置いた。
  - **失うもの**：正解(迷い)が緑になるため、**目盛りだけでは復習対象の総数が読めない**（橙は誤答のみ）。
    home の「復習カード ◯枚」の行がその数を持つので情報自体は失われないが、読み取りが2箇所に分かれる。
- `SessionRecord` に1問ごとの結果（`marks`）を追加する方針で確定。**実施は段階5**（2026-08-17）→ spec.md §9-2・§13-7 c
  §13-7 c の home 図は前回セットの目盛りを描いているが、§9-2 の `SessionRecord` は
  score / accuracyRate / instantRate までで**1問ごとの結果を持たない**。本数は復元できても並び順は復元できず、
  位置＝問番号という意味が失われる。`SCHEMA_VERSION` を上げるかの判断と spec.md 更新が絡むため段階5 でまとめて行う。
- **`SessionRecord` に `marks` を追加する案は見送り**（2026-08-17・上の項目を撤回）→ spec.md §13-8
  締切（2026-08-19）までの時間を優先した。home の目盛りは正解率・即答率から**本数だけ**を復元し、
  即答 → 迷い → 誤答の順に並べる（`lib/marks.ts` の `restoreScoreMarks`）。
  - **失うもの**：位置＝問番号の意味。**同じセッションでも result と home で目盛りの並びが一致しない。**
    result の `緑緑緑▁▁▁緑▁▁▁` が home では `緑緑緑緑▁▁▁▁▁▁` になる。
    直前に result を見た人には食い違いとして目に入るが、これは不整合ではなく仕様とする。
    home の目盛りは「前回どうだったか」の要約であり、1問ごとの復習をする場所は `/review`。
- **AI 生成中に「もう1セット」を押したときは応答を破棄する**（2026-08-17）→ spec.md §12-6 c
  §12-6 c が STEP 6 着手時の判断として保留していたもの。`AbortController.abort()` で捨てる。
  - **代償**：押すたびに **API 呼び出しが1回無駄になり、復習機会も1回減る。** カードは pending のまま残る。
  - **それでも破棄する理由**：裏で生かすと応答の到着先が「もう存在しないセッション」になる。
    卒業済みでないか・cause が変わっていないかの判定と、2セット目の書き込みとの競合を、
    締切までの日数で正しく作り切れない。**壊れた反映より、説明が1回遅れるほうが安い。**
- **プロンプトに cause の内部値を渡さない**（2026-08-17）→ spec.md §10-2
  実機で `pattern_summary` に「（hesitant）」が出た。`formatTarget` と原因の内訳が
  内部値をそのまま渡していたのが原因。`causeLabel()` を通した日本語ラベルで渡すよう変更し、
  禁止事項にも「内部の識別子を出力に含めない」を明記した（二重の歯止め）。
- **動かすものを2つ → 4つに変更**（2026-08-17）→ spec.md §13-10 a
  `result` の到着時に限り、`ScoreStrip` の目盛りの立ち上がりと SCORE の数字の到着を足す。
  **このアプリは計器であり、計器は静かだが値が確定した瞬間に針は振れる。**
  目盛りが得点そのものである以上（§13-8）、確定の瞬間に立ち上がるのは装飾ではなく計器の挙動である。
  - **quiz の進捗と home の前回セットでは動かさない。** 前者は進捗であって計測結果ではなく、
    後者は過去の記録であって、いま測ったものではない。同じ部品でも**値が確定した瞬間だけ**動く。
  - カウントアップ・シマー・パルス・スピナーの禁止は維持。差し色も増やさない。
- **`/review` に削除を追加**（2026-08-18・step6.md §3 判断1「読むだけ」を撤回）→ spec.md §12-8
  カードは即答正解でしか卒業しないため、**枚数が増え続けると一覧として機能しなくなる。**
  読む気を失う長さになると、課題③「減っていく実感」の狙いが逆に潰れる。
  - **消すのはカードだけ。** `wordStats` にも出題対象にも触らない。
    また間違えれば `applySessionToCards` がカードを作り直すので、学習の記録は失われない。
  - **確認ダイアログを出さない。** 取り返しがつく操作であり、
    ダイアログは「重い操作だ」という誤った予告になる。
  - **差し色を使わない**（`--line` の枠と `--text-mute` の文字だけ）。削除は警告ではなく片付け。
  - **消せるのは `/review` だけ。** 結果画面のカードには出さない（読んでいる最中に消す理由がない）。
  - **捨てたもの**：「静かな道具」の一貫性。操作を持たない画面が1つ減った。
    それでも、読めない長さの一覧を放置するほうが道具として悪いと判断した。
- **`review_cards` の優先順位を反転**（2026-08-18）→ spec.md §10-10
  従来は `pending` が先頭だった。**バックログが5件あると、今回まちがえた語に
  今回の説明が1枚も入らない**（`selectCardTargets` を実測して確認）。
  結果画面で読みたいのは、いま解いた語の説明である。
  → **1. 今回の誤答 → 2. 今回の非即答正解 → 3. pending** に変更。
  - **書き戻し側は元から正しかった。** `applyAiResponseToCards` は
    `cards` に存在する id すべてに書くので、今回作ったカードにも届いていた。
    原因は書き戻しではなく**対象の選び方**だった。
  - **代償**：毎回5件以上まちがえ続けると**古い `pending` はいつまでも埋まらない。**
    「次回の呼び出しで埋まる」保証はなくなった。**実装では対処しない。**
    受け皿は §12-8 の手動削除と、即答正解による卒業（§8）。
  - あわせてカードの文言を「説明は次回のセッションで追加されます」→
    **「まだ説明がありません」**に変更（§10-6）。埋まる保証が弱い以上、予告はしない。
  - **採用しなかった案**：上限を 5→10 に上げる。今回の語も pending も全部埋まるが、
    生成量が倍になりレイテンシが 45秒のタイムアウトに触れうる（5枚で実測17〜28秒）。
    再測定のコストに見合わないため見送った。
- **`review_cards` の上限を撤廃し、ストリーミング化した**（2026-08-18）→ spec.md §10-10・§12-6 d・f
  5枚上限のせいで、切られた語に説明が付かないまま `/review` に溜まっていた。
  上限を外し、今回の対象と `pending` を**全件**1回のリクエストに含める。
  待機時間はストリーミングで解いた。**1枚ずつ確定した順に保存・表示する。**
  - **旧設計の懸念は事実に反していた。** §12-6 d は「V2 の幻覚チェックは全件揃ってから」
    としてストリーミングを見送っていたが、V2 は「出題していない id を弾く」検査であり
    **1枚ごとに適用できる**。全件を待つ必要は無かった。
  - **SDK は途中経過をパースしてくれない。** structured outputs の `parsed_output` は
    最終メッセージにしか付かない（型で確認）。増えていく文字列から閉じたオブジェクトだけを
    取り出す純関数（`lib/streamingCards.ts`）を自前で書き、テストで固めた。
  - **打ち切りの意味が変わった。** 1枚ごとに保存するので、
    `FEEDBACK_TIMEOUT_MS` は「全部を失う境界」ではなく「そこで止める時刻」になった。
    45000 → 90000、`maxDuration` 60 → 120。
  - **★実測で分かったこと：待ちの主因は生成量ではなく思考時間。**
    15件で thinking 既定は1枚目 46.7秒 / 全体 60.4秒。`thinking: disabled` にすると
    1枚目 11.4秒 / 全体 39.1秒。**ストリーミングだけでは体感待ちは消えない。**
    thinking を切るかどうかは出力品質に関わるため、判断を保留してある。
- **説明が入っているカードに状態の注記を出さない**（2026-08-18）→ spec.md §10-6
  §11 の「cause が変わったら `pending` に戻す（content は残す）」により、
  説明が読めるカードにも `state==="pending"` が付く。その結果
  「説明を更新中です」が消えずに残っていた。**読める説明があるなら注記は要らない。**
- **AI 呼び出しの thinking を切る**（2026-08-18）→ spec.md §12-6 d
  ストリーミング化しても**1枚目が46.7秒**かかっていた。思考が終わるまで1文字も出ないため。
  `thinking: { type: "disabled" }` で **1枚目 11.4秒 / 全体 39.1秒**（ローカル・15件）。
  - **切ってよい理由**：AI の仕事は「確定した原因を鈴木さんの言葉で説明する」だけ（§10-8）。
    原因の推定も件数の集計もアプリ側が済ませており、深い推論を要さない。
  - **品質の差（同一入力・各1回）**：13枚とも生成。`explanation`+`usage_note` は
    無効1,586字 / 有効1,996字で**有効が約26%長い**。方向は同じで、有効は具体例を添える傾向。
    **1回ずつなので、思考による差か毎回の揺れかは区別できていない。**
  - パラメータ組み立てを `lib/aiRequestParams.ts` に切り出した。
    Route Handler 直書きだと「思考を切っているか」をテストできないため。
- **`FEEDBACK_TIMEOUT_MS` を 90000 → 60000、`maxDuration` を 120 → 90**（2026-08-18）→ spec.md §12-6 f
  思考を切って全体39.1秒になったので、90秒は過大だった。
  **超えても全部を失わない**（1枚ごとに保存済み）ので、余裕を過大に取る意味がない。
  90秒にしても待たされる人が増えるだけになる。
